# Base URL of the VoxAgent backend API (FastAPI), including the /api/v1 prefix.
# The WebSocket telemetry URL is derived from this by swapping http(s) -> ws(s).
VITE_API_BASE_URL=http://localhost:8000/api/v1
